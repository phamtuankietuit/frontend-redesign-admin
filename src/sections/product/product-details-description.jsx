import MarkdownEnhanced from 'src/components/markdown-enhanced';
// ----------------------------------------------------------------------

export function ProductDetailsDescription({ description, sx }) {
  return <MarkdownEnhanced>{description}</MarkdownEnhanced>;
}
